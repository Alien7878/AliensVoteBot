# 👽 AliensVoteBot
### ربات هوشمند و شفاف نظرسنجی تلگرام با امنیت کپچا

[![Python Version](https://img.shields.io/badge/Python-3.10%2B-blue?style=for-the-badge&logo=python)](https://www.python.org)
[![Framework](https://img.shields.io/badge/Framework-Aiogram%203.x-red?style=for-the-badge&logo=telegram)](https://docs.aiogram.dev/)
[![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)](https://opensource.org/licenses/MIT)

ـ **AliensVoteBot** یک ربات تلگرامی پیشرفته و قدرتمند است که با پایتون و کتابخانه `aiogram` برای ساخت، مدیریت و شرکت در نظرسنجی‌ها طراحی شده است. این ربات دارای قابلیت‌های پیشرفته‌ای برای تضمین سلامت آرا و کنترل کامل مدیریت است. رابط کاربری ربات کاملاً فارسی و کاربرپسند طراحی شده است.

---

## 🚀 قابلیت‌های کلیدی

* 🗳️ **ساخت نظرسنجی بی‌نقص**: فرآیند گام‌به‌گام و ساده برای ساخت نظرسنجی با سوالات و گزینه‌های سفارشی.
* 🛡️ **امنیت آرا با کپچا (CAPTCHA)**: برای جلوگیری از فعالیت ربات‌ها و تقلب، کاربران باید قبل از ثبت رأی، کپچاهای ریاضی تصویری را حل کنند.
* 📊 **شفافیت کامل آرا**: امکان اتصال به کانال عمومی برای ثبت لحظه‌ای و ناشناس آرا (با شناسه ماسک شده) جهت اطمینان کاربران از نتایج.
* 🔗 **لینک‌های مستقیم (Deep Linking)**: دسترسی سریع به نظرسنجی‌ها از طریق لینک‌های اختصاصی `t.me/YourBot?start=poll_...`.
* ⚙️ **پنل مدیریت همه‌جانبه**:
    * **مدیریت کاربران و نظرسنجی‌ها**: مشاهده لیست‌های صفحه‌بندی شده از دیتابیس.
    * **سیستم تبلیغات و پیام همگانی**: ارسال یا فوروارد پیام به تمام کاربران با گزارش لحظه‌ای.
    * **عضویت اجباری (Force Join)**: الزام کاربران به عضویت در کانال‌های خاص قبل از استفاده.
    * **لاگ وقایع ادمین**: دریافت گزارش‌های لحظه‌ای از عضویت کاربران جدید و نظرسنجی‌های ساخته شده در کانال خصوصی.

---

## 🛠 ابزارهای فنی (Technical Stack)

* **زبان**: [Python 3](https://www.python.org/)
* **کتابخانه اصلی**: [aiogram 3.x](https://docs.aiogram.dev/)
* **پایگاه داده**: `aiosqlite` (عملیات غیرهمزمان روی SQLite)
* **تولید تصویر**: `Pillow` (برای ساخت کپچاهای اختصاصی)

---

## 📂 ساختار پروژه

```text
├── main.py                 # نقطه ورود و اجرای اصلی ربات
├── config.py               # تنظیمات (توکن، آیدی ادمین و...)
├── database.py             # مدیریت پایگاه داده SQLite
├── captcha_gen.py          # منطق تولید تصاویر کپچا
├── admin_log.py            # ثبت گزارشات برای ادمین در کانال اختصاصی
├── states.py               # وضعیت‌های FSM برای مراحل مختلف
├── requirements.txt        # کتابخانه‌های مورد نیاز
└── handlers/               # پوشه مدیریت دستورات و دکمه‌ها
    ├── admin.py            # منطق پنل مدیریت
    ├── broadcast.py        # سیستم ارسال پیام همگانی
    ├── poll.py             # فرآیند ساخت نظرسنجی
    ├── start.py            # دستور شروع و منوی اصلی
    └── vote.py             # منطق رأی‌گیری و حل کپچا
```
---
## 🚀 راه‌اندازی پروژه (Setup)

فقط با چند قدم ساده می‌تونی بات رو روی سیستم خودت بالا بیاری:

1. **پروژه رو کلون کن**

   ```bash
   git clone https://github.com/alien7878/aliensvotebot.git
   cd aliensvotebot
   ```

2. **نصب پیش‌نیازها**  
>   (توصیه می‌شود داخل محیط مجازی انجام بشه)

  
   # ایجاد محیط مجازی (اختیاری اما توصیه می‌شود)
   ```bash
   python -m venv venv
   source venv/bin/activate    # لینوکس / مک
   venv\Scripts\activate       # ویندوز
   ```

   # نصب پکیج‌ها
   ```bash
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

4. **تنظیم فایل کانفیگ**

   فایل `config.py` را باز کن و مقادیر را با اطلاعات خودت جایگزین کن:

   ```bash
   # config.py
   BOT_TOKEN  = "1234567890:AAF1b2C3d4e5f6g7h8i9j0kLmNoPqRsTuVwX"   # توکن بات تلگرام
   ADMIN_ID   = 987654321                                             # آیدی عددی ادمین (عدد)
   DB_PATH    = "database.db"                                         # مسیر دیتابیس (می‌تونی تغییر ندی)
   CAPTCHA_COUNT = 3                                                  # تعداد کپچاهایی که کاربر باید حل کند
   PER_PAGE   = 10                                                    # تعداد آیتم در هر صفحه پنل ادمین
   ```


5. **اجرا کردن بات**

   ```bash
   python main.py
   ```

   یا اگر از محیط مجازی استفاده کردی:

   ```bash
   python3 main.py
   ```

**تبریک!** 🎉  
بات با موفقیت بالا اومد.  
فایل `database.db` (در صورت نبود) به‌صورت خودکار ساخته می‌شه.

---

# 👽 ساخته شده با ❤️ توسط
